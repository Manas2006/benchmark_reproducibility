name: benchmark-reproducibility
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - lighteval==0.10.0
      - lm-eval==0.4.0
      - torch==2.2.2
      - pyyaml==6.0.1
      - transformers>=4.51.0
      - numpy==1.26.4
 